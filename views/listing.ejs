<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WanderLust</title>
</head>
<body>
    <h1>All Listings :-</h1>
    <form action="/listings/new" method="get">
        <button>Create new listings</button>
    </form>
    <% for( list of data) { %>
        <div>
        
            
            <a href="http://localhost:3000/listings/<%= list._id %>"><%= list.title %></a>
            <p> <%= list.description %></p>
            <h5><%= list.price %></h5>
            <p><%= list.country %> & <%= list.location %></p>
        </div>
    
    <% } %>
</body>
</html>